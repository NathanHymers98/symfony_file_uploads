{% extends 'content_base.html.twig' %}

{% block content_body %}
    <h1>Edit the Article! 🚀</h1>

    <form method="POST" action="{{ path('upload_test') }}" enctype="multipart/form-data"> {# We need the "enctype" part because a file upload is more than just it's contents, we also want to send some metadata
                                                                                            and the browser and the server needs to be able to understand it #}
        <input type="file" name="image">

        <button type="submit" class="btn btn-primary">Upload</button>
    </form>

    <hr>

    {{ include('article_admin/_form.html.twig', {
        button_text: 'Update!'
    }) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script src="{{ asset('js/admin_article_form.js') }}"></script>
{% endblock %}
